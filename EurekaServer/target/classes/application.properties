server.port=8761
spring.application.name=eureka-server

eureka.client.register-with-eureka=false
eureka.client.fetch-registry=false
eureka.instance.hostname=localhost

# Security configuration
spring.security.user.name=sulkar
spring.security.user.password=sulkar19

# Eureka instance configuration to increase refresh rate
eureka.instance.lease-renewal-interval-in-seconds=300
eureka.instance.lease-expiration-duration-in-seconds=600
